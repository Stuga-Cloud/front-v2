export default async function ContainerNewPage({
    searchParams,
}: {
    searchParams?: { [key: string]: string | string[] | undefined };
}) {
    // TODO: au taff willy
    return (
        <>
            <div className="z-20">Create Container</div>
        </>
    );
}
